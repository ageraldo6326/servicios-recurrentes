@extends('layouts.app') @section('content') @include('partials.heading',['heading'=>'Registrar pago'])<form class="panel max-w-xl space-y-4" method="post" action="{{ route('payments.store') }}">@csrf<label>Monto recibido<input class="input" type="number" step="0.01" name="amount" required></label><label>Moneda<input class="input" name="currency" value="USD" maxlength="3" required></label><label>Fecha<input class="input" type="date" name="received_at" value="{{ now()->toDateString() }}" required></label><label>Evidencia (ruta o referencia)<input class="input" name="evidence_path"></label><label>Cobro a imputar<select class="input" name="charge_id"><option value="">Sin imputar todavía</option>@foreach($charges as $charge)<option value="{{ $charge->id }}" @selected(request('charge_id') == $charge->id)>{{ $charge->contractedService->client->name }} · vence {{ $charge->due_date->format('d/m/Y') }} · {{ $charge->currency }} {{ $charge->amount }}</option>@endforeach</select></label><label>Monto imputado<input class="input" type="number" step="0.01" name="allocation_amount"></label><button class="button">Guardar pendiente de validación</button></form>@endsection
