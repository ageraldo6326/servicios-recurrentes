@extends('layouts.app') @section('content') @include('partials.heading',['heading'=>'Pagos recibidos','action'=>route('payments.create')]) @include('partials.list-search',['placeholder'=>'Buscar por moneda o estado','value'=>$search])<div class="surface overflow-hidden"><div class="overflow-x-auto"><table class="table"><thead><tr><th>Fecha</th><th>Monto</th><th>Estado</th><th>Cobros aplicados</th><th></th></tr></thead><tbody>@forelse($payments as $payment)<tr><td>{{ $payment->received_at->format('d/m/Y') }}</td><td>{{ $payment->currency }} {{ number_format($payment->amount,2) }}</td><td>{{ $payment->status->value }}</td><td>{{ $payment->charges->count() }}</td><td>@if($payment->status->value==='pending')<form method="post" action="{{ route('payments.validate',$payment) }}">@csrf<button class="text-emerald-700">Validar</button></form>@endif</td></tr>@empty<tr><td colspan="5" class="muted">No hay pagos que coincidan.</td></tr>@endforelse</tbody></table></div><div class="px-4 py-3">{{ $payments->links() }}</div></div>@endsection
