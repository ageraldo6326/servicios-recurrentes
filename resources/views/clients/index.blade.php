@extends('layouts.app') @section('content') @include('partials.heading',['heading'=>'Clientes','action'=>route('clients.create')]) @include('partials.list-search',['placeholder'=>'Buscar por nombre o teléfono','value'=>$search])<div class="surface overflow-hidden"><div class="overflow-x-auto"><table class="table"><thead><tr><th>Nombre</th><th>Teléfono</th><th>Servicios</th><th></th></tr></thead><tbody>@forelse($clients as $client)<tr><td>{{ $client->name }}</td><td>{{ $client->phone }}</td><td>{{ $client->contracted_services_count }}</td><td><a wire:navigate class="link" href="{{ route('clients.edit',$client) }}">Editar</a></td></tr>@empty<tr><td colspan="4" class="muted">No hay clientes que coincidan.</td></tr>@endforelse</tbody></table></div><div class="px-4 py-3">{{ $clients->links() }}</div></div>@endsection
