@extends('layouts.app')
@section('content')
<div class="mb-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="mb-2 text-xs font-bold uppercase tracking-[0.18em] text-brand">Módulo comercial</p><h1 class="text-2xl font-black tracking-tight text-ink sm:text-4xl">Dashboard comercial</h1><p class="mt-2 text-sm text-muted">Una vista rápida de tu actividad comercial.</p></div><div class="flex gap-2"><a class="button-secondary" href="{{ route('commercial.quotes.create') }}">＋ Cotización</a><a class="button" href="{{ route('commercial.invoices.create') }}">＋ Factura</a></div></div>
<div class="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><div class="card"><p class="text-xs font-bold uppercase tracking-wider text-muted">Facturas este mes</p><p class="mt-3 text-3xl font-black text-ink">{{ $invoicesThisMonth }}</p></div><div class="card"><p class="text-xs font-bold uppercase tracking-wider text-muted">Monto facturado</p><p class="mt-3 text-3xl font-black text-ink">USD {{ number_format($billedThisMonth, 2) }}</p></div><div class="card"><p class="text-xs font-bold uppercase tracking-wider text-muted">Cotizaciones abiertas</p><p class="mt-3 text-3xl font-black text-brand">{{ $openQuotes }}</p></div><div class="card"><p class="text-xs font-bold uppercase tracking-wider text-muted">Cotizaciones aceptadas</p><p class="mt-3 text-3xl font-black text-emerald-700 dark:text-emerald-300">{{ $acceptedQuotes }}</p></div></div>
<div class="mt-6 grid gap-6 xl:grid-cols-2"><section class="panel"><div class="mb-4 flex items-center justify-between"><h2 class="section-title">Últimas cotizaciones</h2><a class="font-bold text-brand" href="{{ route('commercial.quotes.index') }}">Ver todas →</a></div>@forelse($latestQuotes as $quote)<div class="row flex items-center justify-between gap-4"><div><p class="font-bold text-ink">{{ $quote->number }}</p><p class="text-xs text-muted">{{ $quote->client->name }} · {{ $quote->issue_date->format('d/m/Y') }}</p></div><span class="rounded-full bg-brand/10 px-2 py-1 text-xs font-bold text-brand">{{ $quote->status->label() }}</span></div>@empty<p class="muted">Aún no hay cotizaciones.</p>@endforelse</section><section class="panel"><div class="mb-4 flex items-center justify-between"><h2 class="section-title">Últimas facturas</h2><a class="font-bold text-brand" href="{{ route('commercial.invoices.index') }}">Ver todas →</a></div>@forelse($latestInvoices as $invoice)<div class="row flex items-center justify-between gap-4"><div><p class="font-bold text-ink">{{ $invoice->number }}</p><p class="text-xs text-muted">{{ $invoice->client->name }} · {{ $invoice->issue_date->format('d/m/Y') }}</p></div><span class="font-bold text-ink">{{ $invoice->currency }} {{ number_format($invoice->total, 2) }}</span></div>@empty<p class="muted">Aún no hay facturas.</p>@endforelse</section></div>
@endsection
