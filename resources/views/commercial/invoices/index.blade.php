@extends('layouts.app') @section('content')
<div class="mb-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="mb-2 text-xs font-bold uppercase tracking-[0.18em] text-brand">Comercial</p><h1 class="text-2xl font-black tracking-tight text-ink sm:text-4xl">Facturas</h1><p class="mt-2 text-sm text-muted">Administra tus documentos comerciales y su historial.</p></div><a class="button" href="{{ route('commercial.invoices.create') }}">＋ Nueva factura</a></div>
<form class="panel mb-5 flex gap-3" method="get"><input class="input mt-0 flex-1" name="search" value="{{ request('search') }}" placeholder="Buscar por número o cliente"><button class="button-secondary">Buscar</button></form><div class="surface overflow-hidden"><div class="overflow-x-auto"><table class="table"><thead><tr><th>Número</th><th>Cliente</th><th>Fecha</th><th>Total</th><th>Estado</th><th></th></tr></thead><tbody>@forelse($invoices as $invoice)<tr><td class="font-bold text-ink">{{ $invoice->number }}</td><td>{{ $invoice->client->name }}</td><td>{{ $invoice->issue_date->format('d/m/Y') }}</td><td class="font-bold">{{ $invoice->currency }} {{ number_format($invoice->total, 2) }}</td><td><span class="rounded-full bg-brand/10 px-2 py-1 text-xs font-bold text-brand">{{ $invoice->status->label() }}</span></td><td><a class="font-bold text-brand" href="{{ route('commercial.invoices.edit', $invoice) }}">Abrir →</a></td></tr>@empty<tr><td colspan="6" class="py-12 text-center text-muted">No hay facturas que coincidan.</td></tr>@endforelse</tbody></table></div><div class="border-t border-line px-4 py-3">{{ $invoices->links() }}</div></div>
@endsection
