@extends('layouts.app') @section('content') @include('partials.heading',['heading'=>$service->exists?'Editar servicio contratado':'Nuevo servicio contratado'])<form class="panel grid max-w-4xl gap-4 md:grid-cols-2" method="post" action="{{ $service->exists?route('contracted-services.update',$service):route('contracted-services.store') }}">@csrf @if($service->exists)@method('put')@endif<label>Cliente<select class="input" name="client_id" required>@foreach($clients as $client)<option value="{{ $client->id }}" @selected(old('client_id',$service->client_id)==$client->id)>{{ $client->name }}</option>@endforeach</select></label><label>Servicio<select class="input" name="catalog_service_id" required>@foreach($catalogServices as $item)<option value="{{ $item->id }}" @selected(old('catalog_service_id',$service->catalog_service_id)==$item->id)>{{ $item->name }}</option>@endforeach</select></label><label>Proveedor<select class="input" name="provider_id" required>@foreach($providers as $provider)<option value="{{ $provider->id }}" @selected(old('provider_id',$service->provider_id)==$provider->id)>{{ $provider->name }}</option>@endforeach</select></label><label>Precio<input class="input" type="number" step="0.01" name="price" value="{{ old('price',$service->price) }}" required></label><label>Moneda precio<input class="input" name="price_currency" maxlength="3" value="{{ old('price_currency',$service->price_currency) }}" required></label><label>Costo<input class="input" type="number" step="0.01" name="cost" value="{{ old('cost',$service->cost) }}" required></label><label>Moneda costo<input class="input" name="cost_currency" maxlength="3" value="{{ old('cost_currency',$service->cost_currency) }}" required></label><label>IP opcional<input class="input" name="ip" value="{{ old('ip',$service->ip) }}"></label><label>Día de cobro<input class="input" type="number" min="1" max="31" name="billing_day" value="{{ old('billing_day',$service->billing_day) }}" required></label><label>Inicio<input class="input" type="date" name="starts_at" value="{{ old('starts_at',$service->starts_at?->format('Y-m-d')) }}" required></label><label>Estado<div class="input bg-slate-100">{{ $service->status?->value === 'cancelled' ? 'Cancelado' : 'Activo' }}</div></label><label class="md:col-span-2">Observaciones<textarea class="input" name="observations">{{ old('observations',$service->observations) }}</textarea></label><button class="button md:col-span-2">Guardar</button></form>@endsection
