@extends('layouts.app') @section('content') @include('partials.heading',['heading'=>'Servicios contratados','action'=>route('contracted-services.create')])<div class="panel"><table class="table"><thead><tr><th>Cliente</th><th>Servicio</th><th>Proveedor</th><th>IP</th><th>Precio</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>@forelse($services as $service)<tr><td>{{ $service->client->name }}</td><td>{{ $service->catalogService->name }}</td><td>{{ $service->provider->name }}</td><td>{{ $service->ip ?: '—' }}</td><td>{{ $service->price_currency }} {{ number_format($service->price,2) }}</td><td>{{ $service->status->value }}</td><td class="space-x-2"><a class="link" href="{{ route('contracted-services.edit',$service) }}">Editar</a>@if($service->status->value==='active')<form class="inline" method="post" action="{{ route('contracted-services.cancel',$service) }}">@csrf<input class="input inline-block w-40" name="cancellation_reason" placeholder="Razón" required><button class="text-red-600" onclick="return confirm('¿Cancelar inmediatamente?')">Cancelar</button></form>@endif</td></tr>@empty<tr><td colspan="7" class="muted">No hay servicios contratados.</td></tr>@endforelse</tbody></table>{{ $services->links() }}</div>@endsection
