@extends('layouts.app') @section('content') @include('partials.heading',['heading'=>'Registrar gestión'])<form class="panel max-w-2xl space-y-4" method="post" action="{{ route('gestions.store') }}">@csrf<label>Cliente<select class="input" name="client_id" required>@foreach($clients as $client)<option value="{{ $client->id }}" @selected(old('client_id', request('client_id')) == $client->id)>{{ $client->name }} · {{ $client->phone }}</option>@endforeach</select></label><label>Servicio contratado<select class="input" name="contracted_service_id" required>@foreach($services as $service)<option value="{{ $service->id }}" @selected(old('contracted_service_id', request('contracted_service_id')) == $service->id)>{{ $service->client->name }} · {{ $service->catalogService->name }}</option>@endforeach</select></label><label>Tipo<input class="input" name="type" value="{{ old('type', 'WhatsApp') }}" required></label><label>Fecha y hora<input class="input" type="datetime-local" name="occurred_at" value="{{ old('occurred_at', now()->format('Y-m-d\TH:i')) }}" required></label><label>Resultado<input class="input" name="result" value="{{ old('result') }}" required></label><label>Teléfono usado<input class="input" name="phone_used" value="{{ old('phone_used') }}"></label><label>Promesa de pago<input class="input" type="date" name="promised_payment_date" value="{{ old('promised_payment_date') }}"></label><label>Próximo seguimiento<input class="input" type="datetime-local" name="next_follow_up_at" value="{{ old('next_follow_up_at') }}"></label><label>Observaciones<textarea class="input" name="observations">{{ old('observations') }}</textarea></label><button class="button">Guardar gestión</button></form>@endsection
