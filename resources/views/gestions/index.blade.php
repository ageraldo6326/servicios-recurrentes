@extends('layouts.app') @section('content') @include('partials.heading',['heading'=>'Gestiones','action'=>route('gestions.create')]) @include('partials.list-search',['placeholder'=>'Buscar por cliente, tipo o resultado','value'=>$search])<div class="surface overflow-hidden"><div class="overflow-x-auto"><table class="table"><thead><tr><th>Fecha</th><th>Cliente</th><th>Tipo</th><th>Resultado</th><th>Promesa</th></tr></thead><tbody>@forelse($gestions as $gestion)<tr><td>{{ $gestion->occurred_at->format('d/m/Y H:i') }}</td><td>{{ $gestion->client->name }}</td><td>{{ $gestion->type }}</td><td>{{ $gestion->result }}</td><td>{{ $gestion->promised_payment_date?->format('d/m/Y') ?? '—' }}</td></tr>@empty<tr><td colspan="5" class="muted">No hay gestiones que coincidan.</td></tr>@endforelse</tbody></table></div><div class="px-4 py-3">{{ $gestions->links() }}</div></div>@endsection
