@extends('layouts.app') @section('content') @include('partials.heading',['heading'=>'Cobros','action'=>route('charges.create')]) @include('partials.list-search',['placeholder'=>'Buscar por cliente o servicio','value'=>$search])<div class="surface overflow-hidden"><div class="overflow-x-auto"><table class="table"><thead><tr><th>Cliente</th><th>Servicio</th><th>Vence</th><th>Monto</th><th>Estado</th></tr></thead><tbody>@forelse($charges as $charge)<tr><td>{{ $charge->contractedService->client->name }}</td><td>{{ $charge->contractedService->catalogService->name }}</td><td>{{ $charge->due_date->format('d/m/Y') }}</td><td>{{ $charge->currency }} {{ number_format($charge->amount,2) }}</td><td>{{ $charge->status->value }}</td></tr>@empty<tr><td colspan="5" class="muted">No hay cobros que coincidan.</td></tr>@endforelse</tbody></table></div><div class="px-4 py-3">{{ $charges->links() }}</div></div>@endsection
