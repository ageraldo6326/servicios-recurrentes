@extends('layouts.app') @section('content') @include('partials.heading',['heading'=>'Registrar cobro'])<form class="panel max-w-xl space-y-4" method="post" action="{{ route('charges.store') }}">@csrf<label>Servicio contratado<select class="input" name="contracted_service_id" required>@foreach($services as $service)<option value="{{ $service->id }}">{{ $service->client->name }} · {{ $service->catalogService->name }}</option>@endforeach</select></label><label>Monto<input class="input" type="number" step="0.01" name="amount" required></label><label>Moneda<input class="input" name="currency" value="USD" maxlength="3" required></label><label>Fecha<input class="input" type="date" name="due_date" value="{{ now()->toDateString() }}" required></label><input type="hidden" name="status" value="pending"><button class="button">Guardar</button></form>@endsection
