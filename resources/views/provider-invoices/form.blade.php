@extends('layouts.app') @section('content') @include('partials.heading',['heading'=>'Registrar factura de proveedor'])<form class="panel max-w-xl space-y-4" method="post" action="{{ route('provider-invoices.store') }}">@csrf<label>Proveedor<select class="input" name="provider_id" required>@foreach($providers as $provider)<option value="{{ $provider->id }}">{{ $provider->name }}</option>@endforeach</select></label><label>Monto<input class="input" type="number" step="0.01" name="amount" required></label><label>Moneda<input class="input" name="currency" value="USD" maxlength="3" required></label><label>Vencimiento<input class="input" type="date" name="due_date" value="{{ now()->toDateString() }}" required></label><label>Estado<select class="input" name="status"><option value="pending">Pendiente</option><option value="partial">Parcial</option><option value="paid">Pagada</option></select></label><label>Observaciones<textarea class="input" name="observations"></textarea></label><button class="button">Guardar</button></form>@endsection
