@extends('layouts.app') @section('content') @include('partials.heading',['heading'=>'Facturas de proveedores','action'=>route('provider-invoices.create')]) @include('partials.list-search',['placeholder'=>'Buscar por proveedor o estado','value'=>$search])<div class="surface overflow-hidden"><div class="overflow-x-auto"><table class="table"><thead><tr><th>Proveedor</th><th>Vence</th><th>Monto</th><th>Estado</th></tr></thead><tbody>@forelse($invoices as $invoice)<tr><td>{{ $invoice->provider->name }}</td><td>{{ $invoice->due_date->format('d/m/Y') }}</td><td>{{ $invoice->currency }} {{ number_format($invoice->amount,2) }}</td><td>{{ $invoice->status->value }}</td></tr>@empty<tr><td colspan="4" class="muted">No hay facturas que coincidan.</td></tr>@endforelse</tbody></table></div><div class="px-4 py-3">{{ $invoices->links() }}</div></div>@endsection
