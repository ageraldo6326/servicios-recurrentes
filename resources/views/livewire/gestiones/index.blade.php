<div wire:poll.60s>
    <div class="mb-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="mb-2 text-xs font-bold uppercase tracking-[0.18em] text-brand">Historial operativo</p><h1 class="text-2xl font-black tracking-tight text-ink sm:text-4xl">Gestiones</h1><p class="mt-2 text-sm text-muted">Consulta y busca el historial de contacto con tus clientes.</p></div><a wire:navigate href="{{ route('gestions.create') }}" class="button w-full sm:w-auto">＋ Nueva gestión</a></div>
    <div class="panel mb-5"><div class="flex flex-col gap-3 sm:flex-row"><label class="relative flex-1"><span class="sr-only">Buscar gestiones</span><span class="pointer-events-none absolute left-3 top-3 text-lg text-muted">⌕</span><input wire:model.live.debounce.300ms="search" class="input mt-0 pl-10" placeholder="Buscar por cliente, tipo o resultado"></label><button type="button" wire:click="clearSearch" class="button-secondary">Limpiar</button></div><div wire:loading class="mt-3 text-xs font-semibold text-brand">Buscando gestiones…</div></div>
    <div class="surface overflow-hidden"><div class="overflow-x-auto"><table class="table"><thead><tr><th>Fecha</th><th>Cliente</th><th>Tipo</th><th>Resultado</th><th>Promesa</th></tr></thead><tbody>@forelse($gestions as $gestion)<tr><td>{{ $gestion->occurred_at->format('d/m/Y H:i') }}</td><td>{{ $gestion->client->name }}</td><td>{{ $gestion->type }}</td><td>{{ $gestion->result }}</td><td>{{ $gestion->promised_payment_date?->format('d/m/Y') ?? '—' }}</td></tr>@empty<tr><td colspan="5" class="py-12 text-center text-muted">No hay gestiones que coincidan.</td></tr>@endforelse</tbody></table></div><div class="border-t border-line px-4 py-3">{{ $gestions->links() }}</div></div>
</div>
