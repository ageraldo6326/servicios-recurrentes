<div wire:poll.60s>
    <div class="mb-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="mb-2 text-xs font-bold uppercase tracking-[0.18em] text-brand">Control de ingresos</p><h1 class="text-2xl font-black tracking-tight text-ink sm:text-4xl">Cobros</h1><p class="mt-2 text-sm text-muted">Consulta las obligaciones de cobro y sus fechas de vencimiento.</p></div><a wire:navigate href="{{ route('charges.create') }}" class="button w-full sm:w-auto">＋ Nuevo cobro</a></div>
    <div class="panel mb-5"><div class="flex flex-col gap-3 sm:flex-row"><label class="relative flex-1"><span class="sr-only">Buscar cobros</span><span class="pointer-events-none absolute left-3 top-3 text-lg text-muted">⌕</span><input wire:model.live.debounce.300ms="search" class="input mt-0 pl-10" placeholder="Buscar por cliente o servicio"></label><button type="button" wire:click="clearSearch" class="button-secondary">Limpiar</button></div><div wire:loading class="mt-3 text-xs font-semibold text-brand">Buscando cobros…</div></div>
    <div class="surface overflow-hidden"><div class="overflow-x-auto"><table class="table"><thead><tr><th>Cliente</th><th>Servicio</th><th>Vence</th><th>Monto</th><th>Estado</th></tr></thead><tbody>@forelse($charges as $charge)<tr><td>{{ $charge->contractedService->client->name }}</td><td>{{ $charge->contractedService->catalogService->name }}</td><td>{{ $charge->due_date->format('d/m/Y') }}</td><td class="font-bold text-ink">{{ $charge->currency }} {{ number_format($charge->amount,2) }}</td><td><span class="rounded-full px-2.5 py-1 text-[10px] font-black uppercase {{ $charge->status->value === 'paid' ? 'bg-emerald-100 text-emerald-700 dark:bg-emerald-950/50 dark:text-emerald-300' : 'bg-amber-100 text-amber-700 dark:bg-amber-950/50 dark:text-amber-300' }}">{{ $charge->status->value }}</span></td></tr>@empty<tr><td colspan="5" class="py-12 text-center text-muted">No hay cobros que coincidan.</td></tr>@endforelse</tbody></table></div><div class="border-t border-line px-4 py-3">{{ $charges->links() }}</div></div>
</div>
