<div wire:poll.60s>
    <div class="mb-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="mb-2 text-xs font-bold uppercase tracking-[0.18em] text-brand">Relaciones comerciales</p><h1 class="text-2xl font-black tracking-tight text-ink sm:text-4xl">Clientes</h1><p class="mt-2 text-sm text-muted">Administra clientes y consulta sus servicios contratados.</p></div><a wire:navigate href="{{ route('clients.create') }}" class="button w-full sm:w-auto">＋ Nuevo cliente</a></div>
    <div class="panel mb-5"><div class="flex flex-col gap-3 sm:flex-row"><label class="relative flex-1"><span class="sr-only">Buscar clientes</span><span class="pointer-events-none absolute left-3 top-3 text-lg text-muted">⌕</span><input wire:model.live.debounce.300ms="search" class="input mt-0 pl-10" placeholder="Buscar por nombre o teléfono"></label><button type="button" wire:click="clearSearch" class="button-secondary">Limpiar</button></div><div wire:loading class="mt-3 text-xs font-semibold text-brand">Buscando clientes…</div></div>
    <div class="surface overflow-hidden"><div class="hidden overflow-x-auto md:block"><table class="table"><thead><tr><th>Nombre</th><th>Teléfono</th><th>Servicios</th><th>Ingresos</th><th>Costos</th><th>Beneficio neto</th><th>Margen</th><th></th></tr></thead><tbody>@forelse($clients as $client)@php $income = (float) ($client->active_income ?? 0); $cost = (float) ($client->active_cost ?? 0); $profit = $income - $cost; $margin = $income > 0 ? ($profit / $income) * 100 : 0; @endphp<tr><td class="font-bold text-ink">{{ $client->name }}</td><td>{{ $client->phone }}</td><td>{{ $client->contracted_services_count }}</td><td class="font-bold text-ink">USD {{ number_format($income, 2) }}</td><td class="text-sm text-muted">USD {{ number_format($cost, 2) }}</td><td class="font-bold {{ $profit >= 0 ? 'text-emerald-700 dark:text-emerald-300' : 'text-red-600 dark:text-red-300' }}">USD {{ number_format($profit, 2) }}</td><td class="font-bold text-brand">{{ number_format($margin, 2) }}%</td><td><a wire:navigate class="font-bold text-brand hover:underline" href="{{ route('clients.edit',$client) }}">Editar</a></td></tr>@empty<tr><td colspan="8" class="py-12 text-center text-muted">No hay clientes que coincidan.</td></tr>@endforelse</tbody></table></div><div class="divide-y divide-line md:hidden">@forelse($clients as $client)@php $income = (float) ($client->active_income ?? 0); $cost = (float) ($client->active_cost ?? 0); $profit = $income - $cost; $margin = $income > 0 ? ($profit / $income) * 100 : 0; @endphp<article class="p-4"><div class="flex items-start justify-between gap-3"><div class="min-w-0"><p class="truncate font-bold text-ink">{{ $client->name }}</p><p class="mt-1 text-sm text-muted">{{ $client->phone }}</p></div><span class="rounded-full bg-brand/10 px-2 py-1 text-[10px] font-black text-brand">{{ $client->contracted_services_count }} servicios</span></div><div class="mt-4 grid grid-cols-2 gap-3 text-sm"><div><p class="text-xs text-muted">Ingresos</p><p class="font-bold text-ink">USD {{ number_format($income, 2) }}</p></div><div><p class="text-xs text-muted">Costos</p><p class="text-ink">USD {{ number_format($cost, 2) }}</p></div><div><p class="text-xs text-muted">Beneficio neto</p><p class="font-bold {{ $profit >= 0 ? 'text-emerald-700 dark:text-emerald-300' : 'text-red-600 dark:text-red-300' }}">USD {{ number_format($profit, 2) }}</p></div><div><p class="text-xs text-muted">Margen</p><p class="font-bold text-brand">{{ number_format($margin, 2) }}%</p></div></div><a wire:navigate class="mt-4 block text-center font-bold text-brand" href="{{ route('clients.edit',$client) }}">Editar cliente →</a></article>@empty<div class="p-10 text-center text-sm text-muted">No hay clientes que coincidan.</div>@endforelse</div><div class="border-t border-line px-4 py-3">{{ $clients->links() }}</div></div>
</div>
