<div>
    <div class="mb-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between">
        <div><p class="mb-2 text-xs font-bold uppercase tracking-[0.18em] text-brand">Gestión de Compromisos</p><h1 class="text-2xl font-black tracking-tight text-ink sm:text-4xl">Beneficiarios</h1><p class="mt-2 text-sm text-muted">Administra las entidades que reciben los pagos de tus compromisos.</p></div>
        <a wire:navigate href="{{ route('financial-agenda.beneficiaries.create') }}" class="button w-full sm:w-auto">＋ Nuevo beneficiario</a>
    </div>
    <div class="panel mb-5"><div class="flex flex-col gap-3 sm:flex-row"><label class="relative flex-1"><span class="sr-only">Buscar beneficiarios</span><span class="pointer-events-none absolute left-3 top-3 text-lg text-muted">⌕</span><input wire:model.live.debounce.300ms="search" class="input mt-0 pl-10" placeholder="Buscar por nombre, tipo u observaciones..."></label><button type="button" wire:click="clearSearch" class="button-secondary">Limpiar</button></div><div wire:loading class="mt-3 text-xs font-semibold text-brand">Buscando beneficiarios…</div></div>
    <div class="surface overflow-hidden"><div class="hidden overflow-x-auto md:block"><table class="table"><thead><tr><th>Beneficiario</th><th>Tipo</th><th>Compromisos</th><th>Estado</th><th></th></tr></thead><tbody>@forelse($beneficiaries as $beneficiary)<tr><td><p class="font-bold text-ink">{{ $beneficiary->name }}</p><p class="max-w-xs truncate text-xs text-muted">{{ $beneficiary->observations ?: 'Sin observaciones' }}</p></td><td class="text-sm text-muted">{{ $beneficiary->type }}</td><td class="font-bold text-ink">{{ $beneficiary->financial_commitments_count }}</td><td><span class="rounded-full px-2.5 py-1 text-[10px] font-black uppercase {{ $beneficiary->is_active ? 'bg-emerald-100 text-emerald-700 dark:bg-emerald-950/50 dark:text-emerald-300' : 'bg-slate-100 text-slate-500 dark:bg-slate-800 dark:text-slate-300' }}">{{ $beneficiary->is_active ? 'Activo' : 'Inactivo' }}</span></td><td><a wire:navigate class="font-bold text-brand hover:underline" href="{{ route('financial-agenda.beneficiaries.edit', $beneficiary) }}">Editar</a></td></tr>@empty<tr><td colspan="5" class="py-12 text-center text-sm text-muted">No hay beneficiarios registrados.</td></tr>@endforelse</tbody></table></div><div class="divide-y divide-line md:hidden">@forelse($beneficiaries as $beneficiary)<article class="p-4"><div class="flex items-start justify-between gap-3"><div><p class="font-bold text-ink">{{ $beneficiary->name }}</p><p class="text-sm text-muted">{{ $beneficiary->type }}</p></div><span class="rounded-full bg-brand/10 px-2 py-1 text-[10px] font-black text-brand">{{ $beneficiary->financial_commitments_count }} compromisos</span></div><p class="mt-3 text-sm text-muted">{{ $beneficiary->observations ?: 'Sin observaciones' }}</p><a wire:navigate class="mt-4 block font-bold text-brand" href="{{ route('financial-agenda.beneficiaries.edit', $beneficiary) }}">Editar beneficiario →</a></article>@empty<div class="p-10 text-center text-sm text-muted">No hay beneficiarios registrados.</div>@endforelse</div><div class="border-t border-line px-4 py-3">{{ $beneficiaries->links() }}</div></div>
</div>
